[variables]
RUSTFLAGS = '-C link-arg=-fuse-ld=mold'

[phases.build]
nixPkgs = ['...', 'mold']
cmds = ['RUSTFLAGS="-C link-arg=-fuse-ld=mold" cargo build --release']

[start]
runImage = 'debian:bookworm-slim'
onlyIncludeFiles = ['target/release/forum_meucci']
cmd = 'target/release/forum_meucci'